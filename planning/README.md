# Planning

这里保存需求草案、增强方案和候选 backlog 的 HTML 状态页。

优先使用 `index.html` 查看状态。这些文档只能说明“可以考虑做什么”，不能说明“当前代码已经有什么”。进入实现前必须重新读当前源码、补规格和验证计划。

旧 Markdown 方案稿已移入 `../archive/superseded-planning/`，只作为来源材料。新增或修正规划状态时，优先更新本目录的 HTML 状态页。
