# Research

这里保存调研资料的 HTML 状态页。调研内容有参考价值，但可能随代码、脚本和平台状态变化而过时。

优先使用 `index.html` 查看状态。使用这些文档时，必须优先核对当前代码、`docs/ai-workflow/` 和 `qmclient_scripts/` 下的维护入口。

旧 Markdown 调研原文已移入 `../archive/superseded-research/`，只作为来源材料。
