# Superseded Planning Drafts

这里保存从 `规划/` 移出的旧 Markdown 方案稿和候选清单。

这些文件已由 `../../规划/*.html` 状态页接管。它们保留用于追溯旧想法、旧证据和范围来源，但不再作为可直接执行的开发计划。

维护规则：

1. 进入开发前，先读 `../../规划/index.html` 和对应 HTML 状态页。
2. 旧 Markdown 中的路径、配置名、优先级和实现状态都必须重新核对源码。
3. 新规划应写成新的小范围规格或状态页，不继续扩写这些旧草案。



